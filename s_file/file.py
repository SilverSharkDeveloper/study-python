file = open('file1.txt','w',encoding="utf-8")

# open(경로, 방식,encoding="방식")

#방식  = w -> 파일 없으면 만들어주고 열어줌, 있으면 아예 비우고 가져옴
#       a -> 파일 없으면 오류, 있으면 그대로 가져옴
#       r -> 파일 없으면 오류, 있으면 그대로 가져옴, 수정못함

